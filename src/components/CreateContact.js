import React, {useState} from 'react';

const CreateContact = () => {
    const [, setisLoggedIn] = useState(false);
    return(
        <div>
            
        </div>
    );
}

export default CreateContact;